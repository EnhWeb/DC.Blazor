@inherits BlazorToastsBase

@if (ToastList.Any())
{
    <div class="dc-toast-container">
        <CascadingValue Value="this">
            @foreach (var toast in ToastList)
            {
                @toast.Value
            }
        </CascadingValue>
    </div>
}